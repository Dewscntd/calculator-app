export enum OperatorsEnum {
  add = '+',
  subtract = '-',
  divide = '/',
  multi = '*'
}

export enum ActionEnum {
  equals = '=',
  clear = 'AC'
}

export enum CalcButtonEnum {
  digit = 'digit',
  operator = 'operator',
  action = 'action'
}
